datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Plan {
  FREE
  PRO
}

model User {
  id    String @id @default(uuid())
  email String @unique
  plan  Plan   @default(FREE)
}